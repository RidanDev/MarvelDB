<?xml version="1.0" encoding="utf-8"?>
<!-- TODO gestire i caricamenti con Task-->
<!-- TODO non mostrare il nome del personaggio mentre la progressBar è in esecuzione-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">


    <ProgressBar
        android:id="@+id/progress_image_bar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:indeterminate="true"/>

    <ImageView
        android:id="@+id/character_grid_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:scaleType="fitXY"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/character_grid_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_below="@id/character_grid_image"
        android:layout_centerHorizontal="true"
        android:lines="2"
        android:ellipsize="end"
        android:text="character"/>

</RelativeLayout>
    